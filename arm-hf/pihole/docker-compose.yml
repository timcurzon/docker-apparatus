# Nginx docs instance

version: '3'

services:
  dns:
    image: diginc/pi-hole:arm
    env_file: .env
    volumes:
      - $PWD/configs/pihole/:/etc/pihole/
      - $PWD/config/dnsmasq.d/:/etc/dnsmasq.d/
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "8053:80/tcp"
    networks:
      - proxy
      - default

networks:
  proxy:
    external:
      name: proxy
